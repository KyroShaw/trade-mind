# release/ — 发布物料目录

> 由 `/sw-release` 生成，按版本号建子目录，例如 `.wave/release/v0.1.0/`。

## 目录结构（示例）

```
release/
  v0.1.0/
    CHECKLIST.md    # 发布检查清单
    CHANGELOG.md    # 变更日志
    ROLLBACK.md     # 回滚方案
```
